<Page
    x:Class="ChessEngineClient.View.EditPositionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChessEngineClient.EditPosition"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding EditPositionViewModel, Source={StaticResource ViewModelLocator}}"
    xmlns:view="using:ChessEngineClient.View"
    mc:Ignorable="d">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <view:ChessBoardView Margin="0 12" DataContext="{Binding BoardViewModel}" />

        <view:PiecesPaletteView 
            Grid.Row="1"
            Margin="4" 
            DataContext="{Binding PiecesPaletteViewModel}"
            HorizontalAlignment="Center" 
            VerticalAlignment="Top"/>
        
        <CommandBar x:Name="CommandBar" Grid.Row="2">
            <AppBarButton Label="Clear" Icon="Clear" Foreground="Red" Command="{Binding ClearCommand}"/>
            <AppBarButton Label="Done" Icon="Accept" />
            <AppBarButton Label="Cancel" Icon="Cancel" Command="{Binding CancelCommand}"/>
        </CommandBar>
    </Grid>
    
</Page>
