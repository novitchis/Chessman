<UserControl
    x:Class="ChessEngineClient.View.SquareView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="using:ChessEngineClient.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="rootGrid" Background="Transparent">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="DefaultState"/>

                <VisualState x:Name="IsDragSourceState">
                    <VisualState.Setters>
                        <Setter Target="pieceView.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="IsDropTargetState">
                    <VisualState.Setters>
                        <Setter Target="highlightBorder.Background" Value="{StaticResource DropSquareBrush}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="IsDropTargetMagnifiedState">
                    <VisualState.Setters>
                        <Setter Target="highlightBorder.Background" Value="{StaticResource DropSquareBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid x:Name="selectionOverlay"></Grid>
        <Canvas>
            <Border 
                x:Name="highlightBorder"
                CornerRadius="0" />
        </Canvas>
        <Viewbox>
            <!--This is a hack to take up space-->
            <Border Width="20" Height="20"/>
        </Viewbox>
        <view:ChessPieceView x:Name="pieceView" DataContext="{Binding Piece}" />
    </Grid>
</UserControl>
